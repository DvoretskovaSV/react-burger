import {ATTR} from "./elements.sel";

export const CONSTRUCTOR = `[${ATTR}="constructor"]`;
export const CONSTRUCTOR_ITEM_FIRST = `[${ATTR}="constructor-first"]`;
export const CONSTRUCTOR_ITEM_LAST = `[${ATTR}="constructor-last"]`;
export const CONSTRUCTOR_MAIN_INGREDIENTS = `[${ATTR}="constructor-main"]`;
export const CONSTRUCTOR_ITEM = `[${ATTR}="constructor-item"]`;

export const BUTTON_ORDER = `[${ATTR}="order"]`;