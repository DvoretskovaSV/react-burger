import {ATTR} from "./elements.sel";

export const INGREDIENT_ITEM = `[${ATTR}="ingredient-item"]`;
export const INGREDIENT_SECTION = `[${ATTR}="ingredients-section"]`;

export const INGREDIENT_SECTION_BUN = `[${ATTR}="section-0"]`;
export const INGREDIENT_SECTION_MAIN = `[${ATTR}="section-1"]`;
export const INGREDIENT_SECTION_ACTIVE = `active`;
export const INGREDIENT_SECTION_NOT_ACTIVE = `not-active`;

export const TABS = `[${ATTR}="tabs"]`;